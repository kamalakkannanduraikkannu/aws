on: [push]
jobs:
  hello_world_job:
    runs-on: ubuntu-latest
    name: A job to say hello
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - name: 'Coderbyte'
        description: 'Coderbyte action'
        run: sh run.sh
    
